# Copy to .env and source it for local runs, or export directly.
# In GitHub, add this as a repository secret named DISCORD_WEBHOOK_URL.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-id/your-token
